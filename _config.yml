url: "http://slackshop.ru"

# Build settings
markdown: kramdown

collections:
  courses:
    output: true
    permalink: /:path/
  course_reviews:
    output: true
  

defaults:
  - scope:
      path: ""
      type: courses
    values:
      layout: course
